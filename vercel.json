{
    "buildCommand": "npm install && npm install --prifix client && npm run build --prefix client",
    "outputDirectory": "/client/dist",
    "builds": [
      {
        "src": "server/index.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/server/index.js" 
      }
    ]
  }
  
  